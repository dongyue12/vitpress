- 直接通过 HTML 标签名（如 `p`、`h1`、`div`）来选择元素。
- 常用于**全局基础样式设定**或**重置默认样式**，比如统一所有段落的字体、行高等。
- 写法：`标签名 { ... }`
```css
p { font-size: 16px; }
h1, h2, h3 { font-family: sans-serif; }
```
虽然不如类选择器那么精细，但在搭建页面基础风格时仍然很常用。
